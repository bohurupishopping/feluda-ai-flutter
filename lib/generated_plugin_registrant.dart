import 'package:flutter_web_plugins/flutter_web_plugins.dart';

// Removed unused plugin registrations
void registerPlugins(Registrar registrar) {
  // Register only the plugins we're actually using
  // Example: connectivity_plus, file_picker, etc.
} 